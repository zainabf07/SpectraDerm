import type { ReactNode } from "react";
export type Page = "home"|"scan"|"analysis"|"result"|"timeline"|"explanation"|"next"|"products"|"settings";
export function Shell({page,setPage,children}:{page:Page;setPage:(p:Page)=>void;children:ReactNode}) { return <><a className="skip" href="#content">Skip to content</a><header><button className="brand" onClick={()=>setPage("home")} aria-label="SpectraDerm home">SPECTRA<span>DERM</span></button><nav aria-label="Main navigation"><button className={page==="scan"?"active":""} onClick={()=>setPage("scan")}>Start scan</button><button className={page==="timeline"?"active":""} onClick={()=>setPage("timeline")}>History</button><button className={page==="settings"?"active":""} onClick={()=>setPage("settings")}>Privacy</button></nav></header><main id="content">{children}</main><footer>Model-derived observations for skin-health monitoring. Not a medical diagnosis.</footer></> }
