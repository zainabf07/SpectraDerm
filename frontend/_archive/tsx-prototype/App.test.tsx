import { render,screen,waitFor } from "@testing-library/react"; import userEvent from "@testing-library/user-event"; import App from "./App"; import { api } from "./api/client";
vi.mock("./api/client",()=>({api:{createUser:vi.fn().mockResolvedValue({user_id:"u1"}),getUser:vi.fn().mockResolvedValue({user_id:"u1"}),history:vi.fn().mockResolvedValue({timeline:[]}),createScan:vi.fn(),analyze:vi.fn()}}));
describe("home",()=>{it("renders and navigates to scan",async()=>{render(<App/>);expect(screen.getByRole("heading",{name:/understand your skin/i})).toBeInTheDocument();await userEvent.click(screen.getAllByRole("button",{name:/start a scan/i})[0]);expect(screen.getByRole("heading",{name:/start a new observation/i})).toBeInTheDocument()})});
describe("session recovery",()=>{it("replaces a stale backend user automatically",async()=>{localStorage.setItem("spectraderm-user","stale");vi.mocked(api.getUser).mockRejectedValueOnce({status:404});render(<App/>);await waitFor(()=>expect(api.createUser).toHaveBeenCalled());expect(localStorage.getItem("spectraderm-user")).toBe("u1")})});
